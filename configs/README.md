# Configs

Configuration and template files for cloud resources.

## Files

- `gcs_upload.yaml` — Upload/config example for GCS flow.
- `.env.tamplate` — Environment template file (legacy name kept as-is).

Consider copying templates to `.env` (repo root) for local use.
